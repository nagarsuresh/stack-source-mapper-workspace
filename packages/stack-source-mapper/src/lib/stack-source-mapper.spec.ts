import { stackSourceMapper } from './stack-source-mapper';

describe('stackSourceMapper', () => {
  it('should work', () => {
    expect(stackSourceMapper()).toEqual('stack-source-mapper');
  });
});
